apiVersion: v1
kind: Service
metadata:
  name: {{ include "lock-unlock-rewrite.fullname" . }}
  labels:
    {{- include "lock-unlock-rewrite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lock-unlock-rewrite.selectorLabels" . | nindent 4 }}
